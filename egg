repeat
    task.wait(1)
until game:IsLoaded()
task.wait(15)
queue_on_teleport(game:HttpGet("https://raw.githubusercontent.com/notanymore101010/sat/refs/heads/main/egg"))
function Check()
    for _,v in pairs(workspace:GetChildren()) do
        for _,v2 in pairs(game:GetService("ReplicatedStorage").props:GetChildren()) do
            if string.find(v.Name, v2.Name) then
                return true
            end
        end
    end
    return false
end

function CollectEgg()
    if not game:IsLoaded() then game.Loaded:Wait() end
    local player = game.Players.LocalPlayer
    local hrp = player.Character.HumanoidRootPart
    local num = 0
    local TeleportService = game:GetService("TeleportService")

    for _,v in pairs(workspace:GetChildren()) do
        for _,v2 in pairs(game:GetService("ReplicatedStorage").props:GetChildren()) do
            if string.find(v.Name, v2.Name) then
                num = num + 1
                hrp.CFrame = v:GetPivot()
                task.wait(1.5)
                for _,desc in pairs(v:GetDescendants()) do
                    if desc:IsA("ProximityPrompt") then
                        fireproximityprompt(desc)
                        task.wait(1.5)
                    end
                end
            end
        end
    end
    player:Kick("\nRejoining..")
    TeleportService:Teleport(game.PlaceId, player)
end

repeat 
    CollectEgg()
    Check()
    task.wait(1)
until not Check()
